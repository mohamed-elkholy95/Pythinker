# AGENTS.md

Folder-level policy for agent workflow code.

- Use Docker CLI by default for Docker operations and container log access.
- Use Docker MCP (`mcp__MCP_DOCKER__docker`) only when CLI is unavailable or MCP-specific integration is explicitly required.
