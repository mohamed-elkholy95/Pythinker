# AgentSkills standard (Anthropic open standard for portable skill packaging)
skills-ref>=0.1.0

# Core web framework
fastapi[standard]>=0.115.0
uvicorn>=0.32.0

# LLM providers (let browser-use control openai version)
openai>=2.7.2
instructor>=1.7.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Configuration
python-dotenv>=1.0.0

# Async/SSE (let browser-use control httpx version)
sse-starlette>=2.1.0
httpx>=0.28.1

# Utilities (let browser-use control rich and markdownify)
tenacity>=8.2.0
rich>=14.0.0
markdownify>=1.2.0
rapidfuzz>=3.9.0
# 6.7.4+ currently pulls an editdistpy build path that fails in fresh CI images
# with "ModuleNotFoundError: pkg_resources". Pin to known-good version.
symspellpy==6.7.3

# Browser automation
playwright>=1.48.0
browser-use>=0.11.0

# Web scraping with TLS fingerprinting and stealth capabilities
# [fetchers] extra adds curl_cffi (Tier 1) + patchright (Tier 2-3)
scrapling[fetchers]>=0.4

# Docker SDK
docker>=7.1.0

# WebSockets
websockets>=13.1

# Database clients
motor>=3.6.0
pymongo>=4.10.0
beanie>=1.27.0
redis>=5.2.0
qdrant-client>=1.12.0

# Object storage
minio>=7.2.0

# Image processing (screenshot deduplication & WebP thumbnails)
Pillow>=11.1.0
imagehash>=4.3.1

# Caching
async-lru>=2.0.0

# Search & Retrieval
rank-bm25>=0.2.2
sentence-transformers>=3.0.0  # Phase 3: Self-hosted reranking
torch>=2.2.0  # Required for sentence-transformers
numpy>=1.24.0  # Required for MMR diversification

# Hallucination detection (replaces CoVe LLM-based verification)
lettucedetect>=0.1.0  # ModernBERT token-level hallucination detector

# Deal Scraper: RSS feed parsing + structured data extraction
feedparser>=6.0
extruct>=0.17

# File uploads
python-multipart>=0.0.12

# MCP protocol
mcp>=1.9.0

# Security
pyjwt[crypto]>=2.9.0
cryptography>=43.0.0

# Token counting
tiktoken>=0.8.0

# TOTP authentication
pyotp>=2.9.0

# Observability
prometheus_client>=0.21.0
structlog>=24.4.0

# OpenTelemetry distributed tracing (optional — graceful no-op when not installed)
opentelemetry-api>=1.29.0
opentelemetry-sdk>=1.29.0
opentelemetry-exporter-otlp-proto-grpc>=1.29.0
opentelemetry-instrumentation-httpx>=0.50b0
opentelemetry-instrumentation-fastapi>=0.50b0

# Knowledge Base (RAG-Anything multimodal document processing)
# Base package only — MinerU/LightRAG are transitive dependencies; imported lazily.
# Enable with KNOWLEDGE_BASE_ENABLED=true in .env
raganything>=1.2.0

# Prompt optimization pipeline (Thompson sampling + MIPROv2)
# Enable with FEATURE_PROMPT_OPTIMIZATION_PIPELINE=true
dspy>=2.6.0
