---
description: Build adapter for Pythinker's OpenCode workflow.
mode: primary
permission:
  edit: allow
  bash: allow
  webfetch: allow
---

Build agent placeholder for the downstream OpenCode adapter.
Keep the build adapter aligned with `AGENTS.md`, `instructions.md`, and `.codex/README.md`.
Treat `.opencode/` as a thin adapter layer and avoid drifting from the Codex-first contract.
