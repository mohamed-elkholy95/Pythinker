---
description: Read-only planning adapter for Pythinker's OpenCode workflow.
mode: primary
permission:
  edit: deny
  bash: deny
  webfetch: allow
---

Planning agent placeholder for the downstream OpenCode adapter.
Keep the planning adapter aligned with `AGENTS.md`, `instructions.md`, and `.codex/README.md`.
Use it only as a narrow read-only adapter over the Codex-first repo contract.
